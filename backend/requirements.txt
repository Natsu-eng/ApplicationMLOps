# Index CPU dédié de PyTorch (lu par pip, PAS un commentaire) — pour
# torch/torchvision plus bas : garantit une roue CPU, jamais une roue CUDA
# par défaut (plusieurs Go, inutile — aucun GPU dans docker-compose.yml).
--extra-index-url https://download.pytorch.org/whl/cpu

# ── Web framework ─────────────────────────────────────────────
fastapi==0.136.1
uvicorn[standard]==0.46.0
gunicorn==23.0.0

# ── Base de données ───────────────────────────────────────────
sqlalchemy==2.0.49
psycopg2-binary==2.9.11
# Lot 1.1 (correctif C3, AUDIT_DATALAB_2026-08-16.md) — remplace
# `_add_column_if_missing` (migration additive maison, incapable de
# renommer/supprimer/revenir en arrière). Seule dépendance standard pour
# des migrations de schéma SQLAlchemy versionnées ; écrire l'équivalent à
# la main serait moins fiable et non testé par une communauté externe.
alembic==1.19.1

# ── Configuration ─────────────────────────────────────────────
pydantic==2.13.3
pydantic-settings==2.10.1
python-dotenv==1.2.1
email-validator==2.3.0

# ── Auth / Sécurité (Lot 1) ─────────────────────────────────────
python-jose[cryptography]==3.5.0
bcrypt==5.0.0
python-multipart==0.0.27

# ── Datasets tabulaires (Lot 2) ──────────────────────────────────
pandas==2.2.2
numpy==1.26.4
openpyxl==3.1.2
pyarrow==14.0.2

# ── Entraînement ML (Lot 3) ───────────────────────────────────────
scikit-learn==1.3.2
lightgbm==4.3.0
xgboost==2.0.3
catboost==1.2.5
optuna==3.6.1
shap==0.45.0

# ── File de tâches (Lot 3) ────────────────────────────────────────
redis==5.0.4
rq==1.16.2

# ── ML non supervisé — réduction de dimension (Lot 13) ────────────────
# Épinglé à 0.5.6 volontairement (pas la dernière, 0.5.12) : les releases
# umap-learn >=0.5.7 exigent scikit-learn>=1.6, ce qui aurait forcé une
# mise à niveau majeure de scikit-learn (1.3.2 -> 1.9.x) sur tout le
# projet — risque de régression sur le pipeline supervisé/clustering déjà
# testé (SHAP, modèles joblib persistés). 0.5.6 ne demande que
# scikit-learn>=0.22, compatible tel quel, vérifié par un fit réel.
umap-learn==0.5.6

# ── Pilier Vision — dataset image (Lot 15 sous-lot A) ──────────────────
# Déjà présent comme dépendance transitive (12.3.0) — épinglé explicitement
# car désormais une dépendance directe (services/vision_datasets.py).
pillow==12.3.0

# ── Pilier Vision — classification/transfer learning (Lot 15 sous-lot B) ──
# CPU only : aucun GPU dans docker-compose.yml (voir AUDIT_ROADMAP.md,
# diagnostic du chantier Vision) — installé depuis l'index CPU dédié de
# PyTorch (~125 Mo pour torch seul, à inclure dans le Dockerfile backend :
# `--index-url https://download.pytorch.org/whl/cpu`).
torch==2.13.0
torchvision==0.28.0

# ── Observabilité (Lot 4, correctif I7) ───────────────────────────────
# Métriques HTTP exposées en /metrics (format texte Prometheus standard,
# scrappable par n'importe quel collecteur compatible — pas de dépendance
# à une infra Prometheus précise pour que l'endpoint existe).
prometheus-client==0.26.0
# Sentry — actif seulement si SENTRY_DSN est défini (voir api/main.py) :
# ne change rien au comportement tant que la variable est absente.
sentry-sdk[fastapi]==2.68.0

# ── Tests ──────────────────────────────────────────────────────────
pytest==8.2.2
httpx==0.27.2
